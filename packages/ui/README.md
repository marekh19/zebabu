# UI

Reusable Svelte primitives and design tokens for Zebabu applications.

Consume components through explicit package subpaths such as `@zebabu/ui/button`. Keep application shell, domain components, translations, data access, and business rules in their owning application or domain module.
