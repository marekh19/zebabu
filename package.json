{
  "name": "zebabu-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint:check": "turbo lint:check",
    "lint:fix": "turbo lint:fix",
    "format:check": "prettier --check .",
    "format:fix": "prettier --write .",
    "typecheck": "turbo typecheck",
    "knip": "turbo knip",
    "clean": "turbo clean && rm -rf node_modules",
    "db:start": "docker compose up -d",
    "db:push": "bun run --filter @zebabu/web db:push",
    "db:generate": "bun run --filter @zebabu/web db:generate",
    "db:migrate": "bun run --filter @zebabu/web db:migrate",
    "db:studio": "bun run --filter @zebabu/web db:studio",
    "redis:studio": "bun run --filter @zebabu/web redis:studio",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "husky": "^9.1.7",
    "knip": "^6.12.1",
    "lint-staged": "^17.3.0",
    "prettier": "^3.9.6",
    "prettier-plugin-organize-imports": "^4.3.0",
    "prettier-plugin-svelte": "^4.1.1",
    "prettier-plugin-tailwindcss": "^0.8.1",
    "turbo": "^2.10.11"
  },
  "engines": {
    "bun": ">=1.4.0"
  },
  "engineStrict": true,
  "packageManager": "bun@1.4.0",
  "overrides": {
    "zod": "~4.3.6"
  }
}
