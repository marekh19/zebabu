# Reuse Categories across Budgets

Categories belong to a User and are reusable across Budgets. BudgetCategory links a Category to one Budget and owns Budget-specific ordering and Transactions. This replaces the original specification's Budget-owned Category model so Users can maintain one Category catalog and reuse it without duplicating Category metadata for every Budget.
